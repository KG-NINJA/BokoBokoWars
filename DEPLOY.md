# GitHub Pages Deployment

Copy this folder's contents to a repository root, then select that branch and `/ (root)` under Settings > Pages.

No build command is required. The AI strategist feature runs fully in the browser. If Chrome Built-in AI reports `downloadable`, model setup starts only after the user presses the preparation button.
