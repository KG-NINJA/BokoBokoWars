# GitHub Pages Deployment

Copy this folder's contents to a repository root, then select that branch and `/ (root)` under Settings > Pages.

No build command is required. The AI strategist feature runs fully in the browser and falls back automatically when Chrome Built-in AI is unavailable.
